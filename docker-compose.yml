version: '3.9'
services:
  api:
    build: ./packages/api
    ports:
      - "127.0.0.1:3000:3000"
    env_file: .env
    volumes:
      - ipfsdata:/app/.helia
    dns:
      - 8.8.8.8
      - 1.1.1.1
    restart: unless-stopped

volumes:
  ipfsdata:
